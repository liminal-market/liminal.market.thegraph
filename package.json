{
  "name": "order-executed",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ liminal-market/liminal-market",
    "create-local": "graph create --node http://localhost:8020/ liminal-market/order-executed",
    "remove-local": "graph remove --node http://localhost:8020/ liminal-market/order-executed",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 liminal-market/order-executed",
    "test": "jest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
